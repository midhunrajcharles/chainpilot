// Export the chains and Privy app ID from privy config
export { supportedChains, somniaTestnet, anvil, privyAppId } from "./privy";

// Export central API config and endpoint helpers
export { API_CONFIG, endpoints } from "./api";
export type { } from "./api";
